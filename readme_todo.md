## Demo[TO-DO]
To test your BBOP installation and familiarize yourself with the pipeline and data formatting, we provide a demo dataset.

**Download demo data:**
```bash
# TODO: Add download links once demo data is hosted
wget -O demo_data.zip [LINK_TO_DEMO_DATA]
unzip demo_data.zip -d demo/
```

**Run the demo pipeline:**
```bash
cd demo/
jupyter notebook pipeline_demo.ipynb
```

The demo will process one session through the complete pipeline and should take approximately [X] minutes on a standard workstation.
